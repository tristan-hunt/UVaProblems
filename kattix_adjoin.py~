import sys
def load():
	info = next(sys.stdin).split()
	c = int(info[0])
	l = int(info[1])
	for i in range(0, l):
		edges = next(sys.stdin).split()
		edges = [int(e) for e in edges]

for (c, l, edges) in load():
	
